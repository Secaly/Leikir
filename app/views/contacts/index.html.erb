<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <%= contact.first_name %>
      <%= contact.last_name %>
      <%= link_to "edit", contact %>
    </li>
  <% end %>
</ul>

<%= link_to "New contact", new_contact_path %>
